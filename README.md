# IR Assignment 1

## Setup

```bash
python -m venv .venv && source .venv/bin/activate
pip install -r requirements.txt
```

## Reproduce

```bash
make all
```

See `config/mind.yaml` and `config/ebnerd.yaml` for dataset paths, split dates, and hyperparameters.
